const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../doctors';

describe('core.actions.doctors', () => {
  it('should do something');
});
