const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../region';

describe('core.actions.region', () => {
  it('should do something');
});
