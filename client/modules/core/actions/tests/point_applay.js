const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../point_applay';

describe('core.actions.point_applay', () => {
  it('should do something');
});
