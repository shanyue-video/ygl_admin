const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../login_form';

describe('wx.actions.login_form', () => {
  it('should do something');
});
